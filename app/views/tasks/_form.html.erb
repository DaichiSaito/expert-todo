<div class="row">
  <aside class="col-md-12">
    <section class="task_form">
      <%= form_for(@task) do |f| %>
        <%= render 'shared/error_messages', object: f.object %>
        <div class="form-group">
          <%= f.label :content %>
          <%= f.text_area :content, class: 'form-control' %>
        </div>
        
        <div class="form-group">
          <%= f.label :description %>
          <%= f.text_area :description, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :deadline %>
          <%= f.date_select :deadline, use_month_numbers: true, include_blank: true %>
        </div>

<%#
=begin
%>
        <div class="form-group">
          <%= f.label :attachments %>
          <%= f.fields_for :attachments do |attachment| %>
            <%= attachment.file_field :location, multiple: true, name: "attachments[location][]" %>
          <% end %>
        </div>
<%#
=end
%>

<%
=begin
%>
        <% if @task.attachments.present? %>
          <% @task.attachments.each do |attachment| %>
            <%= image_tag attachment.location %>
          <% end %>
        <% end %>
<%
=end
%>
        <!--<input type="file" name="attachments[]" multiple accept="image/*">-->
        
        <%= f.submit "Post", class: "btn btn-primary" %>
      <% end %>
    </section>
  </aside>
</div>